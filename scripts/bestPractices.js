//Best Practices
// Use camelCase for Naming

let firstName, lastName, userInfo, userEmail;
firstName = '';
lastName = '';
userInfo = '';
userEmail = '';

// Variable declaration on top and initialize
// Avoide unnecessary variables
// switch,case, default
// object and array with const

// == vs ===
//access dom less
const ol = document.querySelector('.student-list');
const firstOl = ol.querySelector('li'); //instead of doing const firstOl = document.querySelector('.student-list li')
